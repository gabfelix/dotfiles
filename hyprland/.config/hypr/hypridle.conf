general {
	lock_cmd = hyprlock
	before_sleep_cmd = loginctl lock-session
	after_sleep_cmd = hyprctl dispatch dpms on # avoid having to press a key twice to turn on
}

listener {
	timeout = 150
}

listener {
    timeout = 151                      # 5min
    on-timeout = loginctl lock-session # lock screen when timeout has passed
}

listener {
    timeout = 151
    on-timeout = loginctl lock-session
}
