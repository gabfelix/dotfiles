#!/bin/sh

# Uses yt-dlp to get the channel id of a given youtube URL.

url="$1"

channel_id="$(yt-dlp --print channel_url --playlist-items 1 "$url" | sed 's/https\:\/\/www\.youtube\.com\/channel\///')"
echo "https://www.youtube.com/feeds/videos.xml?channel_id=${channel_id}"
